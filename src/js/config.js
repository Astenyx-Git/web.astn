// config.js — Global configuration constants

export const ASTN_EDIT_MODE = 0; // 0 = Read-only mode (default) — no export button, content not editable
                                  // 1 = Edit mode — export button shown, content editable, save available
